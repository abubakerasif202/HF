interface:
  display_name: "HF"
  short_description: "Repo-specific patterns and workflows for HF"
  default_prompt: "Use the HF repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true